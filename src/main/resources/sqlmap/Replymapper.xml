<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.class4.reply.mapper.ReplyMapper">  
  
  <insert id="replyRegist">
  	insert into Reply(rno, id, bno, content, regdate)
  	values(seq_rno.nextval(), #{id}, #{bno}, #{content}, sysdate)
  </insert>
  
  
  
</mapper>  