<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
<mapper namespace="com.class4.reviewboard.mapper.ReviewBoardMapper">
  	
   <select id="getList" resultType="reviewBoardVO">
	  	<![CDATA[
	  	select *
		from(
			select rownum as rn, a.*
				from(select * from REVIEW
		
		]]>
		
			<if test="searchType=='genre' ">where genre like '%'||#{searchName}||'%'</if>
		    <if test="searchType=='title' ">where title like '%'||#{searchName}||'%'</if>
          	<if test="searchType=='director' ">where director like '%'||#{searchName}||'%'</if>
            <if test="searchType=='actor' ">where actor like '%'||#{searchName}||'%' or content like '%'||#{searchName}||'%' </if>
	        <if test="searchType== null or searchType == '' ">where 1 = 1</if>
		    
		<![CDATA[
		    order by bno desc) a
		) where rn > (#{pageNum}-1) * #{amount} and rn <= #{pageNum} * #{amount}
  		
  		]]>
   </select>
   
   <select id="getTotal" resultType="int">
		select count(*) as total 
		from REVIEW
		<if test="searchType=='title' ">where title like '%'||#{searchName}||'%'</if>
		<if test="searchType=='writer' ">where writer like '%'||#{searchName}||'%'</if>
		<if test="searchType=='content' ">where content like '%'||#{searchName}||'%'</if>
		<if test="searchType=='titcont' ">where title like '%'||#{searchName}||'%' or content like '%'||#{searchName}||'%' </if>
		<if test="searchType== null or searchType == '' ">where 1 = 1</if>
	</select>
	
	<select id="getContent" resultType="reviewBoardVO">
		select * from review where bno = #{bno};
	</select>
	
 
  
</mapper>
